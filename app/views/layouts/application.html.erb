<!DOCTYPE html>
<html>
<head>
  <title>GameTracker</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <div class="content-wrapper">
    <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to 'Game Tracker', root_path, class: "navbar-brand" %>
          <!--<a class="navbar-brand" href="#">Game Tracker</a>-->
        </div>
        <div id="navbar" class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <li class="<%= 'active' if params[:controller] == 'welcome' %>"><%= link_to 'Home', root_path %></li>
            <li class="<%= 'active' if params[:controller] == 'games' %>"><%= link_to 'Games', games_path %></li>
            <li class="<%= 'active' if params[:controller] == 'players' %>"><%= link_to 'Players', players_path %></li>
            <li class="<%= 'active' if params[:controller] == 'game/sessions' %>"><%= link_to 'Sessions', index_game_sessions_path %></li>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container">
      <div class="template">
        <%= yield %>
      </div>
    </div>
  </div>

</body>
</html>
